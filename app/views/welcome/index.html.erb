<div class="container">
	<div class="padding">

	  <% flash.each do |name, msg| %>
	    <div class="alert <%= name == 'error' ? 'alert-danger' : 'alert-info' %>">
	      <div class="container">
	        <%= msg %>
	      </div>
	    </div>
  	<% end %>
  	
		<p>You're on the Welcome Page</p>
		<p>Here is where things should route to by default</p>
	</div>
</div>	